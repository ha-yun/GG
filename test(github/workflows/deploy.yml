# name: Docker, Ci/Cd, EC2

# on:
#   push:
#     branches:
#       - deploy-test

# jobs:
#   docker-cicd:
#     runs-on: ubuntu-latest
#     steps:
#       - name: 소스코드 체크아웃
#         uses: actions/checkout@v4
  
#       - name: 현재 상황 체크
#         run: |
#             pwd
#             ls
      
#       - name: 도커 작업을 진행하기 위한 액션 적용
#         uses: docker/setup-buildx-action@v3
      
#       - name: 도커 허브 로그인
#         run: echo "${{ secrets.HUB_DOCKER_TOKEN }}" | docker login -u "${{ secrets.HUB_DOCKER_USERNAME }}" --password-stdin
